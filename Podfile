# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MultiSelectTestProj' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MultiSelectTestProj

 target 'MultiSelectTestProjTests' do
    inherit! :search_paths
    # Pods for testing
      pod "OCMock"
      pod "Foundry"
      pod "Quick"
      pod "Nimble"
  end

end
